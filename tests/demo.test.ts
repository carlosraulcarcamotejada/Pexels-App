describe('first', () => { 
    test('should first', () => { 
        const id = '7925859'
        const endPoint = `https://api.pexels.com/v1/photos/${id}`;
        expect(id).toBe('7925859');
     })
 })