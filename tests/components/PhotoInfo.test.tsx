import { render } from "@testing-library/react"
import PhotoInfo from "../../src/components/PhotoInfo"
import React from "react"

describe('Pruebas en <PhotoInfo />', () => { 
    test('debe de hacer match con el snapshot', () => {

       // render(<PhotoInfo  />)
     })
 })